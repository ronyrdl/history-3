# history-3
This program is a basic inventory management system that allows users to add, view,
search, update, and delete products. It also calculates statistics such as total value,
total units, most expensive product, and highest stock. The system uses input validation 
through a custom function and allows saving the inventory data into a CSV file for persistence.
